apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: llm-gateway
resources:
  - namespace.yaml
  - redis.yaml
  - gateway.yaml
configMapGenerator:
  - name: gateway-config
    files:
      - gateway.yaml=../config/gateway.yaml
